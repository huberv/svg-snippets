{
  "name": "svg-snippets",
  "version": "1.0.0",
  "description": "My personal collection of SVG-related code snippets",
  "scripts": {
    "start": "browser-sync start --server --files src/*/*.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/huberv/svg-snippets.git"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13"
  }
}
